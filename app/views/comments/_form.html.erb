<%= form_with(model: [@project, @task, Comment.new], local: true) do |form| %>
  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>
  <div class="actions">
    <%= form.submit "Submit" %>
  </div>
<% end %>
