# RSpec.configure do |config|
#   config.include FactoryBot::Syntax::Methods
# end

#In the FactoryBot, we can either use .create def so it creates an object of a model and save it to the test DB,
# or use .build def so it creates the object tho without inserting it into the DB, but instead save it in the memory!
